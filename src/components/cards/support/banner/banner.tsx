import React from "react";
import images from "../../../utls/images";

const Banner = () => {
  return (
    <section>
      <img src={images.support1} className="object-cover" />
    </section>
  );
};

export default Banner;
